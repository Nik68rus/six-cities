export enum AuthorizationStatus {
  Auth = 'Auth',
  NotAuth = 'NotAuth',
}

export enum PropertyType {
  Apartment = 'Apartment',
  Room = 'Private room'
}

export enum Pins {
  Default = '/img/pin.svg',
  Active = '/img/pin-active.svg'
}
